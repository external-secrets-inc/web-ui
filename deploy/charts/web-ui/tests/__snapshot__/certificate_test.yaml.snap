should create a Certificate:
  1: |
    apiVersion: cert-manager.io/v1
    kind: Certificate
    metadata:
      name: RELEASE-NAME-web-ui
      namespace: web-ui
    spec:
      dnsNames:
        - map[host:foo.example.com]
        - map[host:bar.example.com]
      issuerRef:
        kind: ClusterIssuer
        name: example-issuer
      secretName: example-secret
